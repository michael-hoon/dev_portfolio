---
import Icon from "./Icon.astro";
import type { IconRef } from "./icon-types";

interface Props {
	icon: IconRef;
	title: string;
}

const { icon, title } = Astro.props;
---

<div class="header-container">
	<Icon {...icon} width={24} height={24} class="glow-icon" />
	<h2>{title}</h2>
</div>
